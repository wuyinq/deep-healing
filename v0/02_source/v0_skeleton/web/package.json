{
  "name": "deephealing-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "DeepHealing L1 渲染层（three.js，只消费快照，零业务逻辑）。V0 骨架。",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^7.0.0"
  },
  "engines": {
    "node": ">=22"
  },
  "comment": "three.js 为渲染主选（MIT）。react-three-fiber 保留为 V1 组件化选项；Babylon.js 作对照，见 06/07。"
}
