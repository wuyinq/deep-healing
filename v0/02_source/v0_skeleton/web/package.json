{
  "name": "deephealing-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "DeepHealing L1 渲染层（three.js，只消费快照，零业务逻辑）。M3 实现。",
  "scripts": {
    "dev": "vite",
    "build": "node ../../../node_modules/vite/bin/vite.js build",
    "preview": "vite preview",
    "test": "node --test test/render-client.test.ts",
    "scene:assert": "node scripts/scene_assert.mjs"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^7.0.0"
  },
  "engines": {
    "node": ">=22"
  },
  "comment": "依赖**提升**到 <workspace>/node_modules（D-9）：02_source 内零 node_modules / 零 dist / 零 .build。three.js 为渲染主选（MIT）。"
}
