"""provider 适配器包（**round 3 · 修复迭代 2 新增文件**；既有内核源码零改动）。

为什么新增这个包（Raven N-1 的缓解 ①）：
  确定性闸门的执行体加了 **impl 白名单** —— `module:` 只能指向 `deephealing_kernel.*`
  且模块文件必须位于 kernel 根之内（拒绝标准库 / `__main__` / 任意 `sys.path` 模块）。
  于是「正例需要一个真可执行的确定性 provider」只能落在**新增文件**里，
  而不能去改既有内核源码（既有内核文件必须字节不变，见 `03` 的指纹对照）。

本包只放**纯函数**适配器：无随机、无时钟、无网络、无文件系统写入。
"""
