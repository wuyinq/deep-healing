# 00 — 来源清单（M5 保真事实的锚点来源）

- 生成时间: 2026-09-23 15:45 (Asia/Shanghai)
- 生成者: pm-orchestrator (lanova) —— **PM 侧只读取证**，不是小队自述
- 用途: 满足 REQ-20260923-001 前置 **P0-1**（原著权威来源）。M5 的每条保真事实都必须能回指到本清单中的某一层。
- 作品: 《我的治愈系游戏》 作者 **我会修空调**（阅文集团白金作家），起点中文网 首发，**完本 1030 章**

## 1. 两层来源（层级决定证据强度，不混用）

| 层 | 来源 | 取到的东西 | 可核验方式 | 强度 |
|---|---|---|---|---|
| **L1 官方元数据** | `https://www.qidian.com/book/1025901449`（起点官方书籍页，含完整目录） | 全 1030 章条目：**章号 / 标题 / 章节 URL / 首发时间 / 章节字数**；卷划分（正文卷 107 章免费 / 922 章 VIP） | 原始抓取件哈希 + `chapters.tsv` 逐行可查 + 单章 URL 可点开 | **一级锚点**：官方发布，可被 Sentinel / PM 独立回源 |
| **L2 公开镜像正文** | `https://snwxw.com`（少年文学网，第三方镜像） | 章节**正文**（用于抽取事实） | 镜像自身目录页解析出的 章号→URL 映射 + **与 L1「章节字数」比对**（见 §3） | **二级来源**：仅用于**内部事实抽取**，正文不得进产物 |

**为什么需要 L2**：L1 只有元数据，结构层 / 机制层的具体数值（房号、楼层、友善度、任务等级）在正文里。
**为什么 L2 必须被校验**：镜像可能截断或改动，不能凭「看起来是原文」就当权威 —— 故强制 §3 的字数比对。

## 2. L1 落盘物（本目录）

| 文件 | 内容 | sha256 |
|---|---|---|
| `chapters.tsv` | 1031 行（表头 + 1030 章）：`no / title / cid / url / first_pub / words / volume` | `f0045005539e1a8f7411fccc23c7eca4d6687b4693dcaf1182df619a65974d97` |
| `extract_catalog.py` | 从官方页抽取 `chapters.tsv` 的脚本（可复跑） | `2a6f48666fabd5f26a735347b43e2eea6a8113ad291ac3e32922a2c44606c34d` |
| `probe_text_availability.py` | L2 路径可用性 + 完整性校验探针 | `dc49f287a22d4dbeca710e67265e912c7997738bbc9b7678b23586bb29fb49d9` |
| 官方页原始抓取件 | `/Users/wooyinq/.hermes/profiles/lanova/cache/web/www.qidian.com-39d8cd93ff.md`（285,011 B） | `5196bcad83bd727231a9744d07f5c1ea28ec45494fd413eeff833a04946662db` |

抽取实测读数（`python3 extract_catalog.py`，exit=0）：
`entries_total=1030`、`entries_numbered=999`（最大章号 1000，含重复号）、`free_volume_chapters=107`、`vip_volume_chapters=922`。

## 3. L2 完整性校验（把「二级来源」变成可判定的二级来源）

规则：**镜像正文 CJK 字数** 与 **L1 官方「章节字数」** 偏差 ≤ **±12%** 记 PASS；超容差或取不到 ⇒ 该章标 `UNVERIFIED`，不得作为锚点。

实测（`python3 probe_text_availability.py`，exit=0，通过 4/4）：

| 章 | 官方标题 | 官方字数 | 镜像 CJK | 偏差 | 判定 |
|---|---|---|---|---|---|
| 1 | 第1章 玩游戏吗？玩之前先开颅那种 | 2316 | 2069 | −10.7% | PASS |
| 46 | 第46章 五楼 | 2057 | 1837 | −10.7% | PASS |
| 148 | 第148章 开启学校地图 | 2407 | 2159 | −10.3% | PASS |
| 199 | 第199章 层层恐惧 | 2076 | 1881 | −9.4% | PASS |

偏差稳定在 −10% 一带（镜像去掉了行首缩进 / 空行等排版空白，且官方字数把标点计在内），是**系统性**而非随机丢失 —— 因此用容差判定是合适的。

**取正文的路径（踩过的坑，写清楚免得小队重踩）**：
- 起点官方章节页 **curl 直取不可用**：`http=202`、209 字节，返回的是反爬探针页（`/C2WF946J0/probe.js`），无正文。
- 镜像**必须走渲染型提取器**（`web_extract`，已验证可从脚本调用），**不能**用裸 `curl` + 正则：裸抓 23 KB 的 HTML 里正文混在 `div.content`，
  但按「标题后截断」的朴素正则会把标题行里的书名当成终止符，只切出 ~190 字（本次已实测踩到并修正）。
- **章号→URL 不要算术外推**：实测 ch46→`/read/2/177.html`、ch148→`/read/2/279.html`（偏移 131），
  但 `/read/2/331.html` 是**第199章**（偏移 132）—— 镜像存在跳页。**必须从镜像目录页 `/books/2/` 解析映射**。

## 4. 覆盖范围与已知缺口（如实登记）

| 项 | 读数 | 影响 |
|---|---|---|
| L1 官方章条目 | 1030（999 条带章号） | 结构 / 名称 / 机制**存在性**可锚定 |
| L1 免费卷 | 第 1–106 章（107 条，含 1 条非正文章节） | 结构层事实（楼层 / 房号 / 楼内布局）集中在免费卷，**官方正文公开可读** |
| L1 VIP 卷 | 922 章 | 官方正文需账号；事实抽取依赖 L2 |
| L2 镜像可映射章 | **861 / 999** | **约 138 章在镜像目录里没有** ⇒ 这些章的正文层事实只能标 `UNVERIFIED` 或改由船长提供副本 |

## 5. 使用红线（与 CHG-20260923-004 §4 可翻项 2 一致）

1. **正文不得进产物**：L2 正文只用于「读 → 抽事实 → 写一行事实 + 锚点」。不得把小说段落写进 `02_source`、pack、日志、卡片。
2. **锚点必须写全**：`章号 + 官方章节 URL + 首发时间`（后两者从 `chapters.tsv` 取），必要时附镜像 URL 与校验读数。
3. **无锚点即 `UNVERIFIED`**：`fidelity_lint.py --strict` 下必须判红（REQ AC-1）。
4. **不得对外分发**：本清单与 L2 内容仅限内部项目使用（CHG-20260923-004 A6）。

---

## 6. **L0 权威全本**（2026-09-23 17:0x 新增，船长提供）

船长于 2026-09-23 直接提供**校对版全本 + 番外**，**这是本项目迄今最强的一层来源**，从此不再依赖 L2 镜像猜事实。

| 项 | 值 |
|---|---|
| 文件 | `/Users/wooyinq/.hermes/profiles/lanova/sources/deephealing/《我的治愈系游戏》（校对版全本+番外）.txt` |
| 原始落点 | `/Users/wooyinq/Downloads/《我的治愈系游戏》（校对版全本+番外）.txt`（船长提供，未被改动） |
| 字节 / 字符 | 10,405,597 B / 3,507,459 字符 |
| 编码 | UTF-8（含 BOM 无、CRLF 行尾） |
| **sha256** | `9c8b562e94e01d2162ea20e185168e846d917367ce42723ba967259bab074bbb`（两份副本逐字一致） |
| 覆盖 | **1000 / 1000 章**（第 1~1000 章）+ `番外 预告`；含 `（全书完）` 标记 |
| 校验器 | `verify_l1_fulltext.py`（可复跑，exit=0） |
| 校验读数 | `L1-fulltext-verification.json` |
| **章号 → 锚点索引** | `L1-fulltext-index.tsv`（1000 行：真章号 / 排版章号 / 标题 / 官方 URL / 首发时间 / 官方字数 / 正文字数 / 偏差 / 源文件行区间）**只含锚点与读数，无正文** |

### 6.1 实测读数（`python3 verify_l1_fulltext.py`，exit=0）

| 指标 | 读数 | 与 L2 镜像对比 |
|---|---|---|
| 章覆盖 | **1000 / 1000** | L2 只有 861 / 999 ⇒ **补齐 139 章** |
| 逐章完整性偏差 | **中位 +4.3%**，区间 **[−0.1%, +13.6%]**，超 ±12% 仅 **1 章** | L2 稳定 **−10%**（系统性丢排版） |
| 标题对齐 | exact **845** / suffix-only **137** / other **18** | —— |
| 章号排版错位 | **20 处**（真序号 = 出现次序） | —— |

- `suffix-only` 137 处 = 官方标题末尾的运营话术（`（第三更）`/`（求月票）`/`（感谢…白银盟！）`）在校对版里被剥掉 ⇒ **正文标题更干净，不是缺内容**。
- `other` 18 处逐条列在 `L1-fulltext-verification.json` 的 `title_other_rows`。
- **章号错位**：校对版有一处 −10 与一处 +10 的排版错位（真章 332 标成 322；真章 524~543 标成 534~544）。
  ⇒ **一律以 `L1-fulltext-index.tsv` 的 `no`（真章号）为准**，`labeled_no` 只作校对版原样登记。
  官方目录本身另有一处：第 180 章用中文数字排版（`第一百八十章 …`），已在索引里对齐。

### 6.2 校对版自身的已知差异（**抽事实时必须知道**）

1. **1 条第三方插入广告行**（`【…追书app，野果阅读…yeguoyuedu.com】`，源文件偏移 2,973,659）⇒ 校验器已把它从字数里剔除，**抽事实时不得当正文**。
2. **「诡 / 鬼」用字分歧（OPEN，未定案）**：官方目录标题 17 处用「诡」族词（`诡怪` / `诡蜮` / `瑰夫` / `诡母` / `诡匠案` / `诡管理者`…），
   而全本正文与自身标题统一用「鬼」族词（`鬼怪` 1109 / `鬼域` 172 / `鬼母` 141 / `鬼夫` 57 / `鬼匠` 20），
   且正文中「诡」仍正常出现 542 次（`诡异` 431、`诡计` 5）⇒ **不是全局字形替换**，是这两族词的系统性分歧。
   **定案需要官方正文**（免费卷官方章节页本机 curl 与渲染提取**均被反爬拦下**，实测 http=202 探针页 / 提取回落到首页）
   ⇒ **名称表 `04-name-register.md` 两种写法并列登记并标 OPEN，不擅自定案。**
3. 校对版**不含** `番外 终究要长大的小孩`（官方 ext 卷里有，470 字预告 + 1070 字正文）⇒ 该番外仍标 `UNVERIFIED`。

### 6.3 锚点强度（更新后的口径）

| 层 | 用途 | 锚点写法 |
|---|---|---|
| **L0 全本**（新） | **事实抽取的第一来源** | `章号(真) + 官方章节 URL + 首发时间 + 源文件 sha256 + 行区间` |
| L1 官方目录 | 名称 / 结构 / 存在性的**元数据**锚点 | `章号 + URL + 首发时间 + 官方字数` |
| L2 镜像 | **降级为交叉校验**（L0 与 L1 冲突时的第三方旁证） | 镜像 URL + 校验读数 |

`UNVERIFIED` 的适用范围**收窄**为：官方 ext 卷的 30 条运营条目、`番外 终究要长大的小孩`、以及**全本里确实找不到**的事实。

**红线不变**：正文**不得进任何产物**（pack / 日志 / 卡片 / workspace）；只「读 → 抽事实 → 写一行事实 + 锚点」。
本目录新增的 `L1-fulltext-index.tsv` 与校验器**只含标题、数字与行偏移，不含正文**。

