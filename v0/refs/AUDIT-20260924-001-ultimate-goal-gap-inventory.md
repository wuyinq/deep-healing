# 最终目标缺口盘点（AUDIT-20260924-001）

- 作者：lanova（PM）　日期：2026-09-24
- 依据：用户 2026-09-24 指令「沿着我们的最终目标，检查还有哪些事情没有实现，督促实现」
- 口径：**逐项读盘核实**，不采信文档自述。未执行/无证据一律记 **GAP**，不记 PASS。
- 仓库：`/Users/wooyinq/personal/deep-healing` @ `develop`

---

## 0. 最终目标（用户原文锚点）

> 客户端与服务端分离、**写实画风**、从起始街区扩展成**完整世界的 3A 沙盒**；
> 最重要的体验 = 玩家会记住一个 NPC 经历了什么，理解他为什么作出某个选择，并认真考虑自己的行动会怎样影响他；
> NPC 有自己的生活与关系，可拒绝玩家，可在玩家不在场时成长或反复。

---

## 1. 缺口清单（按严重度）

| # | 最终目标项 | 现状（读盘实测） | 判定 | 影响 |
|---|---|---|---|---|
| **G1** | **写实画风** | 交付面场景**全部是 `BoxGeometry`**；`world.ts` 的 `buildEntityBoxes()` 是**唯一装配点**，形状指纹绑死 `BoxGeometry.parameters`；全仓 `grep 'Texture'` → 渲染侧 **0 贴图** | **未实现** | 与「3A 沙盒 / 写实」差一个美术管线；盒体仅可调试 |
| **G2** | **人物形象符合原著** | 人物 = `sizeFor(kind)` 出的**方盒**；内容包 `grep 'appearance\|skin\|hair\|clothes\|gender\|age'` → **0 命中**（无任何外形契约字段） | **未实现**（本轮 PM 已出设计书 + 参考图，见 DESIGN-20260924-001） | 徐琴不可辨识 ⇒ 人物闭环的「看见他」前提不成立 |
| **G3** | **人物闭环玩家可见** | 机制 PASS（给定关键经历时带信号决策 11/31）；但**自然运行**下 `MEMORY_SIGNAL_MIN_IMPORTANCE = 0.5` vs episodic 重要度**实测上限 0.386** ⇒ 实机追溯链恒 `incomplete` | **部分**（内容层缺口） | 玩家看不到「因为那段经历所以改变」 |
| **G4** | **跨多个游戏日** | M5.2 AC-5 实机读数：`ticks_reached: 33`、`run_seconds: 130`。**一个游戏日 = 1440 tick** ⇒ **0.023 天** | **未演示** | 「跨多个游戏日的人物成长」无证据 |
| **G5** | **NPC 有自己生活与关系** | 日程/关系字段在包内存在；行为多样性 AC-10 全绿（室外 32.5%、2/5 NPC ≥20%） | **部分** | 关系对行为的影响未单独验收 |
| **G6** | **玩家可介入 / NPC 可拒绝·误解** | `web/src/ui/participate/intervention.ts` 存在；M5.2 有 with/without 对照臂 | **部分** | 未验「拒绝」与「误解」两类后果是否都可达 |
| **G7** | **世界扩展（起始街区 → 完整世界）** | `districts/` 有 3 个包（`xingfu-xiaoqu` / `-north` / `-xuqin`）；`check_identity_continuity.py` 存在；**但运行时只加载单包** | **设计有、运行时无** | 跨区连续性只有 checker，无真实多区运行 |
| **G8** | **现实事件生成 NPC** | `check_npc_admission_gate.py` 存在（来源/核实/虚构化/时间线检查） | **设计有、未产出** | 用户明确「人物闭环跑通前不批量生成」，故暂缓是对的 |
| **G9** | **架构文档与用户目标一致** | `docs/architecture/01-architecture-design.md:456` 与 `05-risks-and-open-questions.md:18` 的 **R6 仍写「未使用任何原著人物姓名/台词/情节」「只用结构设定」** —— 与用户「1:1 复刻」指令**直接冲突**；工具侧（`scan_ip_boundary.py`）已翻转，**文档未同步** | **未同步** | 文档自相矛盾，后续角色会照旧条文自我设限 |
| **G10** | **美术圣经与原著形象一致** | `art-bible.md` 明文「**高饱和色禁用**（S > 45 不得出现在任何主色板）」+「暖区 20°–60° 为主」；`worldview.schema.json` 的 `saturation_pct` 硬上限 45。而徐琴原著锚点 = **猩红眼眸 / 艳红唇 / 血红衣**（高饱和红） | **硬冲突** | 按现 art-bible，**徐琴无法被正确呈现**；判据会拒掉正确形象 |
| **G11** | **M4 终验 / F7** | F7 未清（fd 泄漏，`SIGABRT`）；M4 **未逐 AC 终验** | **未清** | 继承项，不阻塞人物闭环 |
| **G12** | **客户端与服务端分离** | 内核（Python）+ ws + web 客户端分离，V0 范围内成立 | **基本达成** | — |

---

## 2. 优先级（PM 裁定）

**P0（本轮做）**
- **G2 + G1（人物形象）** —— 用户明确「第一件」。设计已出，实现走小队。
- **G10（美术圣经角色色豁免）** —— 不做，G2 会被自己的判据拒掉。**必须与 G2 同轮**。
- **G9（架构文档 R6 同步）** —— 一句话的事，但不清就是隐患。

**P1（紧随其后）**
- **G3（让关键经历自然产生）** —— 人物闭环真正「玩家可见」的唯一路径。
- **G4（跨日演示）** —— 至少要跑满 ≥2 游戏日（2880 tick）才有资格说「跨日」。

**P2（有前置）**
- G7 多区运行时（前置：G4 跨日稳定）、G6 拒绝/误解后果分叉、G5 关系影响行为。

**暂缓（用户已明确）**
- G8 批量生成新居民 —— 「人物闭环跑通前不要批量生成」。

---

## 3. 明确不做的自我约束（防跑偏）
- **不**把「盒体占位画面」当写实美术达标的证据。
- **不**以「增加测试数量 / 监控指标 / 技术层数」当主要成果。
- **不**用 `healing_delta` 或任务状态跳转代替人物成长。
- **不**在徐琴可信之前批量造新居民。
- **不**把「符合设计」当结论（设计不服务产品目标时，一致一文不值）。
